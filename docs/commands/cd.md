# cd

## Description

Change directory (cd).

Usage: cd [PATH]
If PATH is omitted, change to the user's home directory.

## Help

```
usage: cd [-h] [path]

positional arguments:
  path

options:
  -h, --help
```
